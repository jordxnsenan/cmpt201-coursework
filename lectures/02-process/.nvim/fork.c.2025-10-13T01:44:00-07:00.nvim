#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main(void) {
  pid_t pid = fork();

  for (int i = 0; i < 20; i++) {
    if ( pid == 0 ) {
      printf("CHILD\n");
    }
    printf("Sleeping\n");
    sleep(1);
  }
  printf("DONE\n");

  return 0;
}
